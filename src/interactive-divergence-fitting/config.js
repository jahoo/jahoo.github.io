// ================================================================
//  Interactive divergence fitting — config.js
//  Shared constants imported by the other modules.
// ================================================================

// Display / integration range for the 1D distribution panels.
export const X_MIN = -8;
export const X_MAX = 8;
export const N_GRID = 400;

// Loss-landscape grid resolution: full render vs. low-res while dragging.
export const LAND_RES = 60;
export const LAND_RES_LO = 20;
